spring:
  h2:
    console:
      enabled: false
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://49.247.38.159:3306/shop?serverTimezone=UTC
    username: root
    password: icandoit1!db
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
        default_batch_fetch_size: 1000 # 3
    database-platform: org.hibernate.dialect.MySQL8Dialect
